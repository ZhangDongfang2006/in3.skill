# 2026-09-24

## 08:45 查重补跑（in3-dup-catchup-20260924，一次性任务）✅
昨晚 66c676e3 五连败的补跑。结果：**09-23 已全覆盖，09-24 晨检 0 条，全部报告已发 TG**（msgId 5875/5876/5877）。

### 第 6 次重跑仍超时 → 手动接手收尾
- 08:47 `openclaw cron run` 触发，会话跑了 ~31 分钟后 timeout（error 6x）。导出服务端 08:48:44~08:58:44 已成功，浏览器停在 /tc/list，但会话始终没拿到文件（疑似同样卡在链接提取/下载，或模型调用 stall）
- **接手流程（全程实战验证，已固化进 cron 提示词）**：
  1. 隔离坏文件：昨晚残留的 `物料主数据导出结果-20260923_2006.xlsx` 实为 XML 错误页（1554B），mv 进 `_quarantine/`，防 find_latest_export 误选
  2. **分块提取签名 URL**：evaluate 返回 href 按 22 字符切片数组——整串返回会被传输层省略号截断（`LTAI4F…Qshm`），拼不起来！分块是唯一解
  3. **curl 断点续传循环**：19MB 首次传到 13MB 断（curl error 18），`curl -C -` 续传 1 次即完整（20,934,704B，zip 14 entries 校验过）
  4. 页内点击下载彻底没用：点击后 Chrome 无下载句柄、~/Downloads 无文件（lsof 验证）——**curl 是唯一可行路径**

### 新坑记录
- **~/Downloads 目录枚举挂死**：`ls ~/Downloads`、glob 都 timeout（09:53 起多次复现，与下载无关也挂）。查文件必须 `stat` 精确路径，别 ls
- 浏览器工具对长 opaque 字符串（签名/密钥）会省略号截断，**分块 evaluate 是通用解法**

### 检查结果
- **09-24 晨**：0 条新建/修改（08:49 导出早于工作日开始，正常；今晚 19:30 排程覆盖全天）
- **09-23 补查**（今晨导出筛选 09-23 日期列，84 条 vs 历史 20379 条）：确认重复 **0 对**；待人工确认 **1 对**——DQ23059399 vs DQ23054337 烘箱照明灯 100W φ230，差异仅「防爆」二字（郭运浩 09-23 建/改）
- **09-23 命名检查**：8 条问题——🔴 严重×6：BC01000116~121 铜排半成品（曾庆骄 09-23 建，描述填项目编号 HBD26xxxxx，规则禁止）；💡 提示×2：DQ23059399/DQ23054337 烘箱照明灯制造商空缺
- 文件：`增量查重结果-2026-09-23.xlsx`、`命名规范检查-20260923.xlsx`（ASCII 名副本在 media/）
- 09-23 补查方法：分块日期 symlink（`物料主数据导出结果-2026-09-23.xlsx` 带横线日期触发 cmd_analyze_incremental 的日期正则），查完即删 symlink；命名检查直接 `naming_check.py 2026-09-23`（argv 传日期）

### cron 66c676e3 提示词已加固
- Step 1 加入「下载铁律」：禁 browser download/waitfordownload/页内点击；行文本轮询「已成功」；分块提取链接；curl -C - 续传循环；zipfile 校验；今日完好文件直接复用
- Step 4 补 forceDocument:true；timeout 保持 1800s
- 今晚 19:30 排程正常（error 6x 计数成功一次即清零），观察新提示词首跑

### 待观察
- 今晚 19:30 首跑新提示词效果；若仍超时，下一步考虑把导出+下载也拆成独立 job（同价格分析 Job1/Job2 模式）

## Main 读图能力升级通知（16:40）
今后处理图纸/扫描件（物料清单、价目表扫描件）按此流程：
1. **源头优先**：绝不用低分辨率图——回源 PDF 用 `pdftoppm -png -r 300 xxx.pdf` 重渲（150DPI 小字必糊）
2. **三层识别**：①整页 OCR：`swift /tmp/ocr.swift 图片路径`（Mac Vision 离线免费）抓标签/编号 ②切 6 块（2列×3行、边 20% 重叠）×2 倍放大逐块 OCR 抓小字 ③view_image 整图问结构（glm-4v-flash 可用）
3. **仲裁**：文字以 OCR 为准、结构以视觉为准、冲突标疑问
4. **坑**：view_image 不认 /tmp（图片放工作区）；关键词注意英式拼写（FIBRE/fiber）
Main 假期会把流程固化成技能；当前先手动按流程跑

## 19:00 daily-price-export（采购价格分析 Step 1）
- 宁波导出 ✅：登录态保留；菜单导航正常；Step 2.5 发现湖北残留（孝昌工厂，第 4 次实测）→ 公司级切换宁波 → 核验「宁波工厂」通过；明细视图/展开/取消open PO（onlyOpen:false）/查询/批量导出→导出全部字段（可见项直接点击成功）
- 湖北导出 ✅：公司级切换湖北；/home 卡「加载中」一次，reload×1 恢复（约 8 秒菜单就绪）；核验「孝昌工厂+海越（湖北）电气股份有限公司」通过；同流程导出触发成功
- 备注：本轮 navigate /home 依旧报超时但页面实际加载（与 20260904 记录一致）
