# 2026-05-21 工作记录

## Dongfang 反馈 - 物料重复检查规则优化

Dongfang 看了物料重复检查结果后指出以下问题：

1. **电烙铁 vs 熔锡炉** — 虽然都跟焊接有关，但完全不同的东西，不算重复
2. **油漆刷 vs 铲刀** — 完全不同的工具，不算重复
3. **工作服** — 不用管，跳过
4. **核心原则强调**：对比物料名称时要先判断「是不是同一种东西」，不是同一种东西/用途的肯定不算重复物料

### 行动
- 已将规则更新到 MEMORY.md
- 下次分析脚本需要加入语义预筛：如果两个物料名称属于完全不同的品类/用途，直接判定非重复

## 重构：物料重复检查工具脚本

Dongfang 指出每次都临时写 `compare_results.py` 不好，要求做成稳定工具。

### 成果
- 创建了 `IN3数据/in3_dup_check.py` — 统一 CLI 工具，支持三个子命令：
  - `analyze <输入文件>` — 分析物料主数据，输出可疑重复 Excel
  - `compare <旧文件> <新文件>` — 对比两次结果，生成变更报告
  - `auto` — 自动找最新文件，分析并对比
- 日期从文件名自动提取，不再需要每次改代码
- 所有 42 条规则完整保留
- 旧脚本 `analyze_v3.py` 和 `compare_results.py` 已重命名为 `.legacy`
- 已更新 SKILL.md 和 MEMORY.md

### 验证结果
- `auto` 模式：成功处理 18852 条物料，87 对待人工确认
- `compare` 模式：成功对比两次结果
- 小数点保护已验证：RV 2.5 ≠ RV 25、BV 1.5mm² ≠ BV 15mm² ✅
- cron 任务已更新为使用新工具脚本（不再用旧的 analyze_v3.py）

## 第三轮修正
- 新增：安装方式一边有一边没有 = 非重复（非标箱暗装 vs 非标箱）
- 新增：工作服/服装类直接排除、电烙铁≠熔锡炉、油漆刷≠铲刀/毛刷
- 修复：器具/工具类先过 42 条规则再决定，不再直接跳过
- 结果：87→25→19 对待人工确认

## Dongfang 规则补充
- 多功能表、电能表、电度表 — 描述一样就可能是重复（代码已支持跨名称配对）
- 当前数据中没有描述相同的跨名称配对，但规则已记录备用
