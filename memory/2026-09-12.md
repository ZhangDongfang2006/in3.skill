# 2026-09-12

## 09:00 周六全量物料查重（cron 92ddfa50）✅
- 导出：09:03 触发（44,505 条，已取消过滤成品/虚拟件，URL 参数 excludeCPAndXX:false 确认生效），09:04:07 已成功，下载重命名 `物料主数据导出结果-20260912.xlsx`
- 流程顺：SSO 账密预填直接点登录 → 直达 /mdm/masterdata/search → checkbox[2] 取消过滤 → 搜索 → 批量导出物料（点一次）→ /tc/list 取链接 → curl 下载
- 全量 auto：20,243 条有效，8,672 对候选 → 脚本确认 3 + 待人工 41 + 排除 8,610 + 白名单 18
- AI 复审 41 对：排除 32（0.2S双绕组、II后缀、标识牌文字、10VA、ACS-EXT扩展模块×3、BWKG A/B/C×3、二级能效、SSR-FD、iPRD1/PRF1 r后缀、AC型、III、双头螺纹、/T vs /Q、-J、DFY-2B、高档焊接、半圆/圆、可升降×2、变光、隔板≠断路器、保温棉≠螺纹管、D-URTK≠URTK/S、挡板≠连接片），升级确认 +2（FB 10-RTK/S 同型号仅注释差异；CJ19-6321 上周已确认维持），保留待人工 7（DT-70 vs 70、PMC-851X 4编码×6对，均上周遗留）
- 最终：确认 5 / 待人工 7 / AI排除 32（含原因 sheet）
- 遗留核验（本周 9/7~9/11 每日命名报告 + 上周六遗留 4 项，共 38 编号）：已修正 6（标识牌乘号×4、安全工具柜、GCS柜架项目名）、已删除 3（磁环、烘箱密封条×2）、未修正 29（热缩管乘号×9、电缆命名×8 全是 9/11 新建；上周六遗留 4 项全部未动：按钮×2、操作机构、急停按钮）→ `遗留未修正-2026-09-12.xlsx`
- 送达：TG 5824（摘要）+ 5827/5828（两个 xlsx）

## 经验
- **sendDocument 又踩坑**：ASCII 文件名 + 中文 caption 的 media 发送仍报 `OutboundDeliveryError: Network request for 'sendDocument' failed!`（两次）；加 `forceDocument: true` 后成功（带中文 caption 也成功）。attachments 数组多文件+长中文消息也失败。以后发 xlsx 一律：单文件 media + forceDocument:true，失败再拆分重试
- **导出文件结构**：数据在 `物料主数据` sheet（44506×115），表头带 `*` 前缀（`*物料编号`），说明 sheet 的 dimension 是 A2:B120 假数据；读表时先按 sheetnames 定位再用 lstrip('*') 匹配表头
- 9/10 增量查重的隔板/塑壳断路器对（DQ02059237/DQ02058323）今日复审判非重复（附件≠本体），已在排除 sheet 记录原因

## daily-price-export（19:00 cron）
- ✅ 宁波+湖北采购订单明细导出均触发成功，浏览器已关。
- ⚠️ **DNS 异常（严重）**：in3.industrics.com 解析在 76.223.54.146/13.248.169.48（GoDaddy/Afternic 停放 IP，ERR_SSL_UNRECOGNIZED_NAME_ALERT）与真实 IP 47.103.213.111（阿里云）之间抖动；父域 industrics.com 的 NS 已变为 ns1/ns2.afternic.com，apex 指向停放页（forsale.godaddy.com）。若明日再遇 SSL 错误，等 DNS 抖回真实 IP 后重试即可；持续恶化则需通知管理员（可能域名到期/被卖）。
- 恢复路径复用：/home 卡「加载中」reload×2 无效 → 直接 navigate /spm/purchase-order/list 可加载（20260909 备选方案再次有效，本次两厂均用它）。
- 公司切换器：navbar el-select 用 mousedown+mouseup+click 打开，选项在 body 下的 popper 里。
- 工厂核验链完整：宁波=宁波海越电器制造有限公司/宁波工厂；湖北=海越（湖北）电气股份有限公司/孝昌工厂。

## 采购价格分析 Step2（19:20 cron）
- 双文件下载成功，MD5 不同无串厂；行数+大小双重验证归属（湖北19,966行/7.28MB，宁波30,481行/11.07MB，与昨日吻合）
- 🔑 **重要发现：IN3下载中心导出文件名时间戳是 YYYYDDMM 格式（日月颠倒）**，如 20261209190956458 = 2026-09-12 19:09:56。按 YYYYMMDD 解析会误判为未来日期找不到今日文件
- 结果：铜价偏离0条、历史均价15条、新物料3条（跳过1）
- ⚠️ 电流互感器 LMK3-0.66 50/5 +134.6%：网上比价德力西同规格¥48.13，本次¥46.53与市场持平；历史均价¥19.83疑似杂牌价，建议确认品牌而非砍价
- 时间继电器 JS14P ¥70.04 比价正常；两个抑制柜为非标定制未搜价
- Telegram 已发（msg 5830），飞书按 9/10 指令停发
