# 2026-05-12 工作日志

## 定时任务修复
1. **delivery 配置修复** — `channel: "last"` 改为明确的 `channel: "telegram", to: "8782649356", accountId: "bot8"`
2. **企业名修正** — `bofang` → `haiyue`
3. **登录信息更新** — 确认使用 18392180970 / Start12345@

## 物料重复检查（大改版）

### 执行流程
1. ✅ 登录 IN3 正式环境（haiyue）
2. ✅ 取消过滤成品/虚拟件，导出全部 37,317 条物料
3. ✅ 从 v1（209k对）→ v2（48k对）→ v3-final2（36对确认+107对待人工确认）

### 规则大幅优化（今天 Dongfang 逐条指导）
1. **最高优先级：只要有一项不同 = 非重复**（形状/尺寸/颜色/材料/内外）
2. **型号中数字/字母不同 = 非重复**
3. **名称不同优先处理** — 标准化后比较，有实质差异→非重复/待人工确认
4. **仅符号差异 = 确认重复**（空格/×x/Φφ/括号等）
5. 新增 42 条排除规则（#0-#41）

### 关键 bug 修复
- ultra_normalize 去掉小数点导致 RV2.5=RV25 → 保留数字间小数点
- 名称数字比较缺失 → 新增名称中数字提取
- 螺丝头部形状/表面处理/材质差异未检测
- 柜型型号（GCK/GCS/MNS等）未识别
- 分闸vs合闸、静vs动、明装vs暗装未区分
- 分类逻辑优先级错误：is_naming_mismatch 在名称比较之前执行
- 跨名称配对缺少数组集合校验

### 最终结果
- 确认重复：36 对（全部是仅空格/符号差异的真正重复）
- 待人工确认：107 对（名称不同需人工判断）
- 非重复：623 对（不导出）

### 两次验证
- 同一份数据跑两次：✅ 完全一致
- 5/11 vs 5/12 数据对比：确认重复差1对（新增物料），待人工确认完全一致

## 新建文件
- `电气设备基础知识.md` — 配电柜型号、断路器型号、螺丝知识、元器件类别
- `物料命名规范建议.md` — 符号、数值、型号、描述格式规范（草稿）

## 经验教训
1. **子代理必须主动汇报** — 超过 5 分钟必须 message 汇报，不能等 Dongfang 来问
2. **规则持续积累** — 电气基础知识很重要，需要不断学习
3. **分类逻辑优先级很关键** — 名称比较必须在 is_naming_mismatch 之前
4. **验证很重要** — 每次改动后要和之前结果对比
5. **文件发送路径** — 需先 cp 到 /Users/zhangdongfang/.openclaw/media/ 才能用 message 发送
