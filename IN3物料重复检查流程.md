# IN3 物料重复检查流程

## 一、流程概览

```
登录IN3 → 导出物料主数据 → 数据分析筛选 → 规则分类 → 网上搜索验证 → 生成Excel → 核对准确性 → 交付
```

## 二、详细步骤

### Step 1：登录 IN3 系统
- 打开浏览器访问 https://in3.industrics.com/home
- 企业选 `***ACCOUNT***`，账号 `18392180970`，密码 `Start12345@`
- 页面默认已填好账号密码，直接点登录即可
- 确认已进入正式平台（宁波海越电器制造有限公司 - 宁波工厂）

### Step 2：批量导出物料主数据
- 导航：系统管理 → 主数据管理 → 物料档案管理（MM01）→ 取消勾选「过滤成品/虚拟件」→ 点击搜索 → 批量导出物料
- **⚠️ 必须取消勾选「过滤成品/虚拟件」**，否则导出数据不完整！checkbox 用 evaluate 操作：`document.querySelectorAll('input[type=checkbox]')[2].click()`
- 导出前先检查下载中心是否已有今日导出文件，避免重复
- **只点一次**批量导出，不要重复点击
- 前往下载中心（任务栏），观察任务状态
- **等任务状态变为「已成功」后立即下载**（一般1-3分钟），不需要一直等
- 如果页面看不到任务状态（非全屏），等 **3分钟** 后再去下载
- **只下载一次**，不要重复下载
- 保存到工作区 `IN3数据/` 目录

### Step 3：读取原始数据
- 导出文件结构：第一个 sheet 是"说明"（模板说明），实际数据在 **"物料主数据"** sheet
- 共 114 列，约 19000+ 条物料
- 关键列映射：
  - col2: 物料编号
  - col6: 物料名称
  - col7: 物料描述
  - col9: 物料类别
  - col11: 物料子类别
  - col13: 制造商
  - col14: 物料来源
  - col22: 主计量单位
  - col16: 提前期
  - col61: 标准价格
  - col70: 创建人
  - col72: 创建日期
  - col73: 最近修改人
  - col75: 最近修改日期

### Step 4：筛选可疑重复物料
对全部物料两两比较，筛选物料描述**完全相同或高度相似**的配对，生成可疑重复列表。

**排除类别**：成品柜、外购成套 — 这两类物料的重复检查没有意义，直接排除，不参与对比。

### Step 5：规则分类（自动）
使用以下规则将可疑重复分为"确认重复"、"非重复"、"待人工确认"三类：

#### 核心判断原则（Dongfang 强调）
1. **⚠️ 最高优先级：只要有一项不一样，就是不同的东西，不算重复！** 形状不一致（方形 vs 圆形）、尺寸不一样（200mm vs 300mm）、颜色不一样、材料不一样 — 任何一项不同 = 非重复
2. **⚠️⚠️ 重复强调（多次）：型号中不一致的数字或字母代表了元器件的某些特征（颜色、尺寸、方向、电流、电压等），只要型号中有任何数字/字母不同，且该位代表规格参数 = 非重复！不应该进入待人工确认表！**
3. **不能光比相似度百分比** — 要理解型号含义，型号中每一位数字/字母通常代表具体规格参数（尺寸、颜色、大小、电流等级等），不同就是不同元器件
4. **制造商完全不同 = 非重复** — 除非是命名不规范导致（如"宁波三爱"vs"三爱"、"海越电气"vs"海越湖北"，这种可能是同一家公司不同写法）
5. **材质不同 = 非重复** — 如铜 vs 铝、304不锈钢 vs 316不锈钢
6. **方向或尺寸不同 = 非重复** — 如左操 vs 右操、上进 vs 下进、长度/宽度数值不同
7. **型号中特定数字不同 = 非重复** — 数字代表规格参数，不同数字 = 不同规格 = 不同元器件
8. **颜色不同 = 非重复**

#### 排除规则（算非重复，不算重复）
| # | 规则 | 示例 |
|---|------|------|
| 0 | **⚠️ 只要有一项不同就=不同产品** | 形状（方形vs圆形）、尺寸（200mm vs 300mm）、颜色、材料，任一不同即非重复 |
| 1 | 甲供件不算重复 | 物料类别/子类别包含"甲供" |
| 2 | 一个有制造商一个没有不算重复 | A有制造商，B为空 |
| 3 | **制造商完全不同**（新增核心规则） | A=施耐德，B=正泰 → 非重复；但"宁波三爱"vs"三爱"需进一步确认 |
| 4 | 安装方向不同 | 左操 vs 右操、平进平出 vs 平进侧出、上进 vs 下进 |
| 5 | 断路器壳架等级后字母不同（不同分断能力） | CDM3-63F vs CDM3-63C（F/C/D/H/L/M/N/S） |
| 6 | 微型断路器脱扣曲线不同 | B型 vs C型 vs D型 |
| 7 | 极数不同 | 3P vs 4P、1P vs 1P+N |
| 8 | 颜色不同 | 灰 vs 棕 vs 红等 |
| 9 | 漏电保护类型不同 | A型 vs AC型 |
| 10 | 带附件 vs 不带 | 失压/辅助/报警/门框/电磁锁等 |
| 11 | 互感器型号/窗口尺寸/精度等级不同 | |
| 12 | 电线类型不同 | BV vs BVR、ZR-YJV vs YJV |
| 13 | 保护类型不同 | 变压器保护 vs 线路保护 vs 电容器保护 |
| 14 | 螺纹方向不同 | 正牙 vs 反牙 |
| 15 | 版本不同 | 1A vs 1B |
| 16 | 脱扣方式不同 | TMD(热磁) vs MA(仅电磁) |
| 17 | 脱扣单元不同 | LSI(ABB) vs TMA(施耐德) |
| 18 | 配件不同 | 安全挂锁 vs 相间隔板 |
| 19 | 过电压保护器型号不同 | TBP-A vs TBP-B |
| 20 | 型号系列不同 | CM3E(电子式) vs CM3(普通) |
| 21 | RV vs BVR 电线类型不同 | |
| 22 | **材质不同**（新增） | 铜 vs 铝、304 vs 316不锈钢、紫铜 vs 黄铜 |
| 23 | **尺寸数值不同**（新增） | 型号中代表尺寸的数字不同（如长度、直径、截面） |
| 24 | **额定电流/电压等电气参数不同**（新增） | 100A vs 250A、220V vs 380V、50Hz vs 60Hz |
| 25 | **型号中编码位不同**（新增） | 型号中某一位字母/数字代表特定属性（如安装方式、防护等级），不同就是不同产品 |
| 26 | **成品柜/外购成套不算重复**（新增） | 成品柜与成品柜、外购成套与外购成套，即使描述相同也不算重复（排除类别） |
| 27 | **EPS 等设备功率数字不同 = 非重复**（新增） | EPS 3KW vs EPS 37KW，功率参数不同就是不同产品 |
| 28 | **内 vs 外 = 非重复**（2026-05-12 新增） | 内六角螺丝 vs 外六角螺丝、内牙 vs 外牙，内外结构不同 |
| 29 | **× vs x 等符号差异 = 命名不规范**（2026-05-12 新增） | M8×25 vs M8x25、Φ vs φ 等仅符号不同，算确认重复但标注「命名不规范」 |
| 30 | **名称中数字不同 = 非重复**（2026-05-12 新增） | 24芯光纤 vs 48芯光纤、2.5mm² vs 4mm²，名称中数字代表规格 |
| 31 | **器具/工具类重复 → 待人工确认**（2026-05-12 新增） | 器具、工具、工器具子类别的物料重复不自动判断，需人工确认 |
| 32 | **柜型型号不同 = 非重复**（2026-05-12 新增） | GCK vs GCS vs MNS vs KYN28 等不同柜型是不同产品 |
| 33 | **需掌握电气基础知识**（2026-05-12 新增） | 电气设备基础知识.md：柜子型号、元器件名称、型号规则、参数含义 |
| 34 | **分闸 vs 合闸 = 非重复**（2026-05-12 新增） | 断路器的分闸和合闸是不同功能，完全不同的产品 |
| 35 | **螺丝头部形状不同 = 非重复**（2026-05-12 新增） | 十字/一字/内六角/外六角/梅花/沉头/半圆头等，头部形状不同 = 不同螺丝 |
| 36 | **螺丝表面处理不同 = 非重复**（2026-05-12 新增） | 镀彩/镀白锌/发黑/达克罗/不锈钢等，表面处理不同 = 不同螺丝 |
| 37 | **静插件 vs 动插件 = 非重复**（2026-05-12 新增） | 静/动触头、静/动插件是不同部件 |
| 38 | **母线大小不同 = 非重复**（2026-05-12 新增） | TMY-50×5 vs TMY-60×6，母线截面尺寸不同 = 不同产品 |
| 39 | **尺寸不一致 = 非重复（通用）**（2026-05-12 新增） | 任何物料的尺寸数值不同就是不同产品 |
| 40 | **明装 vs 暗装 = 非重复**（2026-05-12 新增） | 明装（表面安装）vs 暗装（嵌入式安装）= 不同安装方式 = 不同产品 |
| 41 | **安装方式不同 = 非重复**（2026-05-12 新增） | 暗装/户外/户外型/壁挂/落地/嵌墙/挂墙/明装/嵌入式，任一不同 = 非重复 |

#### 确认重复规则
- 描述仅格式/表述差异（空格、单位标注、全角半角、罗马数字等）
- 1N vs 1P+N（施耐德同一极数不同表述）
- 其他经规则确认为同一产品的不同写法

#### 名称不同规则（2026-05-12 Dongfang 强调）
- **名称不一样 + 描述一致** → 归到「待人工确认」区，不自动判断
- 例如：A=信号线端子，B=LED至主板，描述都是「线材/LED至主板/24#4pin线/L=220」→ 需人工确认用途是否相同
- 例如：A=组合螺丝，B=带垫螺丝，描述都是 5×12 → 组合和带垫可能是一回事也可能不同，需人工确认
- 例如：A=U型套管，B=对接套管 → 形状/结构不同，但不确定是否功能不同，需人工确认
- 原因：名称不同可能代表用途不同，但也可能只是命名不规范
- **⚠️ 待人工确认 ≠ 重复**，只是暂时无法自动判断，需要人工最终决定

#### 命名不规范规则（2026-05-12 新增）
- **仅符号/格式差异的 = 确认重复（命名不规范）**，单独列出并标注
- 例如：M8×25 vs M8x25（×和x）、Φ vs φ、全角vs半角括号等
- 这类物料本质是同一个东西，但命名不规范导致出现两条，需要统一规范

#### 待人工确认
- 以上规则都无法自动判断的配对

### Step 6：网上搜索验证（针对待人工确认的配对）
- 对每一对，分别在网上搜索 A 和 B 的型号
- 如果 A 和 B 的型号都能在网上搜到且确认是**不同产品** → 归为非重复
- 如果确认是同一产品的不同写法 → 归为确认重复
- 搜不到或无法判断 → 保持待人工确认

### Step 7：生成 Excel 报告
文件名：`可疑重复物料_已验证.xlsx`

#### Sheet 结构（3个sheet）
1. **确认重复** — 经规则+搜索确认为重复的物料对
2. **非重复** — 经规则排除为不重复的物料对
3. **待人工确认** — 无法自动判断，需人工审核

#### 排版格式
- **交替行**：每对物料一行A一行B，穿插排列
- **背景色区分**：A行浅蓝色(#E8F0FE)，B行浅橙色(#FFF3E0)
- **列1标记**：A-1, B-1, A-2, B-2...
- **同一属性放同一列**：A和B的物料编号放「物料编号」列，A和B的名称放「物料名称」列，依此类推（不是A所有属性一列、B所有属性一列）
- **差异标红**：物料名称/描述/类别/子类别/制造商/来源/单位/提前期/价格 有差异时红色字体
- **不标红的列**：物料编号、创建人、创建日期、最近修改人、最近修改日期 → 始终黑色
- **列宽**：物料描述列宽50字符，备注列宽40字符
- **备注列**：显示排除原因/搜索证据
- **冻结窗格**：冻结前两列+前两行
- **自动筛选**：启用
- **额外信息列**：创建人、创建日期、最近修改人、最近修改日期等也需显示，但如果某列所有物料该属性都为空，则不显示该列

### Step 8：数据准确性核对
- 将 Excel 中每个物料的 14 个字段与 IN3 原始导出文件逐字段核对
- 确保一致性（允许整数/浮点数的显示差异，如 1 vs 1.0）
- 如有不一致，从原始数据修正

### Step 9：交付
- 将 Excel 发送给 Dongfang 审核
- 等待反馈，根据反馈调整规则

## 三、注意事项

1. **环境区分**：正式环境 `***ACCOUNT***`，测试环境 `***ACCOUNT***`，默认操作正式环境
2. **敏感操作**：删除/修改操作需 Dongfang 确认
3. **规则持续优化**：每次收到 Dongfang 的反馈后，更新本文档的排除规则和确认规则
4. **原始数据保留**：从 IN3 下载的原始文件不修改，另存为处理后的文件
5. **物料来源字段**：原始导出文件在"物料主数据" sheet（非"说明" sheet），col14

## 四、历史执行记录

| 日期 | 物料总数 | 可疑重复 | 确认重复 | 非重复 | 待人工确认 | 备注 |
|------|---------|---------|---------|--------|-----------|------|
| 2026-05-07 | 19,063 | 2,327 | 100 | 2,205 | 22 | 首次运行 |
| 2026-05-12 | 37,317 | 48,367 | 587 | 24,316 | 23,464 | 待人工确认过多，需优化匹配逻辑 |

## 五、踩坑记录（持续更新）

| 日期 | 问题 | 原因 | 解决方案 |
|------|------|------|----------|
| 2026-05-09 | GitHub 密码泄露 | 配置文件含明文密码 | git-filter-repo 清理历史，.gitattributes 自动脱敏 |
| 2026-05-09 | 导出后立即检查下载中心 | 文件还没生成 | 等 3-4 分钟后再检查 |
| 2026-05-12 | delivery 发送失败 | channel: "last" 自动解析失败 | 明确指定 channel/to/accountId |
| 2026-05-12 | 企业名 bofang 不存在 | 配置时写错 | 正确企业名 ***ACCOUNT*** |
| 2026-05-12 | 子代理 21 分钟无反馈 | 未主动汇报 | 超过 5 分钟必须 message 汇报 |
| 2026-05-12 | 导出数据不完整 | 忘记取消「过滤成品/虚拟件」 | 进入物料档案管理后第一步就取消勾选 |
| 2026-05-12 | 匹配结果过多 | v1 按类别+子类别分组太宽泛 | v2 按类别+子类别+名称分组，仍需继续优化阈值 |
| 2026-05-12 | 形状不同的拉手被判定为重复 | 缺少"形状不同"规则 | 新增最高优先级规则：只要有一项不同（形状/尺寸/颜色/材料）就是不同产品 |
| 2026-05-12 | RV 2.5 和 RV 25 被判定为重复 | ultra_normalize 去掉小数点导致 2.5=25 | 保留数字间小数点，新增电线截面积比较规则 |
